# [INFRA-002] Mixpanel 초기화 및 수익화 트래킹 로직 삽입

## 1. Task 개요
- **도메인:** Analytics / Business
- **목표:** 비즈니스 지표(B2B 링크 클릭률)를 측정하기 위해 Mixpanel SDK를 연동하고 이벤트를 트래킹합니다.
- **선행 태스크:** FE-012
- **핵심 가치 (VP):** 데이터 기반의 비즈니스 의사결정 및 수익성 검증 (H1)

## 2. Vibe Coding 프롬프트 (AI 입력용)

---
**[Prompt]**
유저가 어떤 프리미엄 링크를 누르는지 모르면 서비스를 개선할 수 없다. 유저의 발자취를 남겨라.

작업 지시:
1. `mixpanel-react-native` 패키지를 설치하고 초기화 코드를 작성하라.
2. `B2BCurationCard` 컴포넌트의 클릭 핸들러 내부에 `mixpanel.track('b2b_link_clicked', { category, title, url })` 로직을 추가하라.
3. 유저가 가입할 때 `mixpanel.identify(user_id)`를 호출하여 행동을 추적 가능하게 하라.
---

## 3. 검증 기준 (QA)
- [ ] 카드를 클릭했을 때 Mixpanel 대시보드에 실시간으로 이벤트가 찍히는가?
- [ ] 유저별로 어떤 카테고리에 관심이 많은지 데이터로 확인 가능한가?
