# [TEST-007] 강제 지연 발생 시 2.5초 내 Fallback 반환 테스트

## 1. Task 개요
- **도메인:** Testing / Stability
- **목표:** 외부 API가 느려지는 상황을 강제로 연출하여, 우리가 설정한 2.5초 타임아웃과 Fallback 메시지가 정확히 작동하는지 통합 테스트합니다.
- **선행 태스크:** AI-005, TEST-001
- **핵심 가치 (VP):** [CP-2] 장애 상황에서의 우아한 대처

## 2. Vibe Coding 프롬프트 (AI 입력용)

---
**[Prompt]**
최악의 상황(서버 렉)에서도 유저는 앱이 고장 났다고 느껴서는 안 된다.

작업 지시:
1. `tests/timeout.test.ts`를 생성하라.
2. OpenAI API 호출을 가로채서(Mocking) 5초간 지연시킨 후 응답하게 하라.
3. 우리의 엣지 함수를 호출했을 때, 2.5초 시점에 자동으로 타임아웃이 걸리고 에러 안내 문구("서버 연결 원활하지 않음")가 반환되는지 시간 측정과 함께 검증하라.
---

## 3. 검증 기준 (QA)
- [ ] 응답 시간이 3초를 넘지 않고 종료되는가?
- [ ] 반환된 응답 내용이 미리 정의된 Fallback 텍스트와 일치하는가?
