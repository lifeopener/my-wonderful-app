# [PUSH-002] 24시간 미접속 유저 추출 쿼리 작성

## 1. Task 개요
- **도메인:** Backend / Logic
- **목표:** `updated_at` 시간이 현재로부터 24시간 이상 지난 유저 목록을 추출하는 관리자용 엣지 함수 로직을 작성합니다.
- **선행 태스크:** PUSH-001
- **핵심 가치 (VP):** [VP-1] 방치된 유저를 챙기는 '평생의 친구' 철학 실현

## 2. Vibe Coding 프롬프트 (AI 입력용)

---
**[Prompt]**
우리 앱을 잊어버린 유저들을 찾아내야 한다. 그들이 우리를 떠나기 전에 먼저 손을 내밀어야 한다.

작업 지시:
1. `supabase/functions/cron-push/index.ts` 라는 새로운 엣지 함수를 생성하라.
2. `profiles` 테이블을 조회하여 `updated_at < now() - interval '24 hours'` 인 유저들의 UUID와 페르소나 설정, 그리고 `fcm_token`을 가져오는 쿼리를 작성하라.
3. 보안을 위해 이 함수는 오직 `service_role` 키로만 접근 가능하도록 설정하라.
---

## 3. 검증 기준 (QA)
- [ ] 쿼리 실행 시 정확히 24시간 동안 활동이 없었던 유저만 리스트업되는가?
- [ ] 푸시를 보낼 수 있는 유효한 `fcm_token`을 가진 유저만 필터링되는가?
