# [AI-004] Vercel AI SDK `streamText` 및 OpenAI 연동

## 1. Task 개요
- **도메인:** LLM
- **목표:** 완성된 프롬프트와 툴들을 가지고 실제로 OpenAI API를 호출하여 실시간 스트리밍 응답을 생성합니다.
- **선행 태스크:** AI-001, AI-002, AI-003
- **핵심 가치 (VP):** 생동감 넘치는 실시간 대화 경험 제공

## 2. Vibe Coding 프롬프트 (AI 입력용)

---
**[Prompt]**
이제 모든 준비가 끝났다. 실제로 유저와 대화할 수 있는 엔진을 돌려야 한다.

작업 지시:
1. `chat/index.ts`에서 Vercel AI SDK의 `streamText`를 호출하라.
2. `model`은 `openai('gpt-4o-mini')`를 사용하라. (비용 효율성)
3. 위에서 만든 `promptBuilder`의 결과물을 `system` 메시지로 넣어라.
4. `tools` 옵션에 `getRealTimeNews`와 `getB2BCuration`을 등록하라.
5. 응답을 클라이언트로 스트리밍(Streaming) 방식으로 반환하라.
---

## 3. 검증 기준 (QA)
- [ ] 유저의 질문에 대해 한 글자씩 실시간으로 응답이 내려오는가?
- [ ] 질문에 따라 적절한 툴(검색 또는 큐레이션)이 자동으로 실행되는가?
