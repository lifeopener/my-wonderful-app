# [PUSH-005] `pg_cron` 스케줄링 등록 (매일 선톡 자동화)

## 1. Task 개요
- **도메인:** Infra / Automation
- **목표:** 수동 호출 없이, 매일 정해진 시간(예: 오전 10시)에 `cron-push` 엣지 함수가 자동으로 실행되도록 Supabase 스케줄러를 설정합니다.
- **선행 태스크:** PUSH-004
- **핵심 가치 (VP):** 끊김 없는 서비스 자동화

## 2. Vibe Coding 프롬프트 (AI 입력용)

---
**[Prompt]**
우리가 매일 버튼을 눌러서 선톡을 보낼 수는 없다. 데이터베이스가 스스로 판단해서 매일 아침 유저들을 챙기게 하라.

작업 지시:
1. Supabase SQL Editor에서 `pg_cron` 익스텐션을 활성화하라.
2. `cron.schedule` 함수를 사용하여, 매일 한국 시간 오전 10시(UTC 01:00)에 `http` 익스텐션으로 우리의 `cron-push` 엣지 함수를 호출하는 작업을 등록하라.
3. 호출 시 `service_role` 키를 헤더에 포함하여 보안 인증을 통과하게 하라.
---

## 3. 검증 기준 (QA)
- [ ] `cron.job` 리스트에 우리의 작업이 정상적으로 등록되었는가?
- [ ] 로그 테이블에서 정해진 시간에 호출 시도가 있었는지 확인 가능한가?
