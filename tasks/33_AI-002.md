# [AI-002] `getB2BCuration` (DB 연동) Tool Calling 로직

## 1. Task 개요
- **도메인:** AI Logic
- **목표:** 유저의 상황에 맞는 실무 자료나 네트워킹 모임을 DB에서 찾아 추천하는 툴을 구현합니다.
- **선행 태스크:** DB-006
- **핵심 가치 (VP):** [VP-3] 실무적 성과 창출 및 BM 수익화

## 2. Vibe Coding 프롬프트 (AI 입력용)

---
**[Prompt]**
유저에게 실질적인 도움을 주는 솔루션을 제공하라. DB에 저장된 B2B 카탈로그를 조회하는 툴이 필요하다.

작업 지시:
1. `chat/tools/`에 `curation.ts`를 생성하라.
2. `getB2BCuration` 툴을 정의하라.
3. `parameters`는 `category` ('education', 'networking', 'investment')를 받도록 하라.
4. `execute` 함수 내에서 Supabase DB의 `b2b_curations` 테이블을 조회하여 해당 카테고리의 링크 목록을 반환하라.
---

## 3. 검증 기준 (QA)
- [ ] 특정 카테고리를 요청했을 때 DB의 데이터가 JSON 형태로 정확히 반환되는가?
- [ ] AI가 답변 중에 이 툴을 사용하여 링크 카드를 추천할 수 있는가?
