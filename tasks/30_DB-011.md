# [DB-011] `get_monthly_cost` 예산 추적 RPC Function 작성

## 1. Task 개요
- **도메인:** Database
- **목표:** 현재 로그인한 사용자의 이번 달 총 API 사용 비용을 합산하여 반환하는 보안이 적용된 RPC 함수를 작성합니다.
- **선행 태스크:** DB-005
- **핵심 가치 (VP):** 5만원 예산 한도 방어 (C-03)

## 2. Vibe Coding 프롬프트 (AI 입력용)

---
**[Prompt]**
우리 앱의 생존을 위해 비용 추적은 실시간으로 정확해야 한다. 엣지 함수에서 안전하게 호출할 수 있는 DB 함수가 필요하다.

작업 지시:
1. `get_monthly_cost` 라는 PostgreSQL 함수를 작성하라.
2. 현재 날짜의 '월'을 기준으로 `api_usage_logs` 테이블에서 현재 유저(`auth.uid()`)의 `estimated_cost_krw` 총합을 계산하라.
3. 이 함수는 `SECURITY DEFINER`로 선언하여 보안을 강화하고, 반환 타입은 `NUMERIC`으로 하라.
4. 마이그레이션 파일로 배포하라.
---

## 3. 검증 기준 (QA)
- [ ] RPC 함수가 현재 달의 데이터만 정확히 필터링하여 합산하는가?
- [ ] 본인의 데이터만 합산하도록 `auth.uid()` 필터가 적용되었는가?
