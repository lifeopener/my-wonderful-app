# [TEST-003] Zod 스키마 Validation 에러 검증 테스트

## 1. Task 개요
- **도메인:** Testing / Logic
- **목표:** 우리가 정의한 Zod 스키마(API-003)가 잘못된 데이터(형식에 맞지 않는 URL, 빈 문자열 등)를 정확히 걸러내는지 검증합니다.
- **선행 태스크:** API-003, TEST-001
- **핵심 가치 (VP):** 데이터 무결성 확보를 통한 시스템 안정성 유지

## 2. Vibe Coding 프롬프트 (AI 입력용)

---
**[Prompt]**
쓰레기가 들어가면 쓰레기가 나온다(GIGO). 잘못된 데이터가 백엔드로 흘러 들어가는 것을 원천 차단해야 한다.

작업 지시:
1. `tests/validation.test.ts`를 생성하라.
2. `B2BCurationDTO` 스키마를 대상으로 테스트를 작성하라.
3. `url` 필드에 "invalid-url"을 넣었을 때 에러가 발생하는지 검증하라.
4. `PersonaSetupDTO`에서 `persona_name`을 빈 문자열로 보냈을 때 에러가 발생하는지 검증하라.
---

## 3. 검증 기준 (QA)
- [ ] 유효하지 않은 데이터 입력 시 Zod가 명확한 에러 메시지를 반환하는가?
- [ ] 유효한 데이터 입력 시에는 에러 없이 통과하는가?
