# [INFRA-003] API 호출 시마다 토큰 비용 적재 로직

## 1. Task 개요
- **도메인:** Backend Logic / Cost
- **목표:** AI 대화가 발생할 때마다 실제 사용된 토큰 수와 예상 비용(원화)을 계산하여 `api_usage_logs` 테이블에 기록합니다.
- **선행 태스크:** DB-005, AI-004
- **핵심 가치 (VP):** 실시간 비용 모니터링 (C-03)

## 2. Vibe Coding 프롬프트 (AI 입력용)

---
**[Prompt]**
비용이 새는 곳을 막아야 한다. 매 대화마다 얼마를 썼는지 장부에 적어라.

작업 지시:
1. `chat/index.ts`에서 LLM 응답이 완료된 후 `usage` 객체(Input/Output tokens)를 추출하라.
2. 현재 환율과 OpenAI 단가를 적용하여 KRW 비용을 계산하는 로직을 추가하라.
3. 결과를 `api_usage_logs` 테이블에 INSERT 하라.
---

## 3. 검증 기준 (QA)
- [ ] 대화 한 번마다 `api_usage_logs`에 새로운 레코드가 생성되는가?
- [ ] 누적된 합계가 실제 OpenAI 청구서와 유사하게 산출되는가?
