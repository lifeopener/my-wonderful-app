# [PUSH-001] 대화 발생 시 `updated_at` 갱신 로직 추가

## 1. Task 개요
- **도메인:** Backend / Activity Tracking
- **목표:** 유저가 채팅을 보낼 때마다 `profiles.updated_at` 컬럼을 현재 시간으로 갱신하여, 나중에 '24시간 미접속자'를 판별할 기준을 만듭니다.
- **선행 태스크:** DB-003, LOGIC-001
- **핵심 가치 (VP):** 옴니채널 일상 교감(선톡)을 위한 데이터 정합성 확보

## 2. Vibe Coding 프롬프트 (AI 입력용)

---
**[Prompt]**
유저가 언제 마지막으로 말을 걸었는지 알아야 우리가 언제 먼저 말을 걸지 결정할 수 있다.

작업 지시:
1. `supabase/functions/chat/index.ts` 파일의 메인 로직에 추가하라.
2. 유저의 메시지를 성공적으로 수신하고 처리하기 시작할 때, `public.profiles` 테이블에서 해당 유저(`auth.uid()`)의 `updated_at` 컬럼을 `now()`로 업데이트하는 SQL 쿼리를 실행하라.
---

## 3. 검증 기준 (QA)
- [ ] 채팅을 한 번 보낼 때마다 DB의 `updated_at` 시간이 실시간으로 바뀌는가?
